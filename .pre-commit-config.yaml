repos:
-   repo: local
    hooks:
    -   id: requirements
        name: requirements
        entry: bash -c 'myenv\Scripts\pip freeze > requirements.txt; git add requirements.txt'
        language: system
        pass_filenames: false
        stages: [commit]